import { styled } from "../../../styles/Theme.provider";

export const ProductPriceText = styled("h1", {
  color: "$text",
  display: "flex",
  alignItems: "center",
  fontSize: "3rem",

  span: {
    marginRight: "1rem",
  },
});
