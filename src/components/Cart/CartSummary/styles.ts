import { styled } from "../../../styles/Theme.provider";
import Flex from "../../Flex";

export const CartSummaryContainer = styled(Flex, {
  boxShadow: "0 0 10px rgba(0,0,0, 0.1)",
  padding: "20px",
  borderRadius: "10px",
  maxHeight: "170px",
  height: "100%",
});
